# F01: Reconcile income surplus using final taxes

Date: 2026-09-08. Pre-freeze implementation receipt. Lifecycle: In build. Hold: Verification. Subsequent exact-commit browser, PR, CI and review evidence belongs in the PR receipt.

## Scope and identity

Tier 3: protected financial/tax behavior. User-authorized implementation order is item 2 (F02), item 1 (F01), then item 4 (F09). Each fix has an independent worktree and branch. The owner subsequently authorized commits, browser verification, push, draft PR publication, review requests and check monitoring. Merge and deployment remain separate decisions.

- Worktree: C:/Dev/Parallax/.worktrees/f01-surplus-cash-20260908
- Branch: codex/f01-surplus-cash-20260908
- Base and current HEAD: 1b636db7a447b57db65d0427e55b2c5a70c138a1, fetched origin/main at preflight.
- Starting worktree: clean. Candidate: uncommitted working-tree changes; no frozen SHA.
- The original C:/Dev/Parallax checkout and its pre-existing dirty files were preserved.
- Source finding: user-provided September 7 engineering-audit excerpt. The original full report and audited SHA remain unavailable; current code was rechecked rather than treating the excerpt as current proof.

## Acceptance ledger

| Request | Disposition | Evidence |
|---|---|---|
| F01: Reconcile income surplus using final taxes | Fixed locally; not Delivered | Fail-before and pass-after below; browser/review/CI pending |
| Other selected findings in F02 -> F01 -> F09 | Separately scoped | Independent sibling worktrees; this receipt covers F01 only |
| F05 known basis and F06 real-dollar basis | Deferred | Not selected for this implementation |
| Earlier Joe screenshot concerns: working-year draw, savings destinations and depletion at 77 | Separately scoped | These three audit fixes are not a claim that every screenshot concern is resolved |

## Production change

Use the complete signed adjusted funding gap when crediting income surplus. Previously the solver clamped away the initial surplus before applying the final-tax adjustment. Retain existing forced-RMD attribution and gross-spending behavior.

Exact code/test files:
- src/projection/engine/federalFunding.js
- test/engine/surplus-cash.test.js
- docs/AUDIT-F01-2026-09-08.md (this receipt)

## Verification

Before: 3 failures and 1 unaffected zero-surplus/deficit pass. With $100,000 opening wealth, $100,000 income, $50,000 spending and $10,000 final tax, a zero preliminary tax rate left wealth at $100,000 instead of $140,000 despite reporting $40,000 net cash flow.

After: Four focused tests pass. Preliminary tax rates of 0%, 10%, 22%, 40% and 60% yield identical actual taxes, cash and wealth. The production federal tax resolver also satisfies the invariant. Returns, asset-sale proceeds, a one-time outlay, zero surplus and deficits reconcile.

- Focused command: `node --test test/engine/surplus-cash.test.js`.
- `npm.cmd test`: 24/24 prerequisite tests and 953/953 discovered unit tests pass.
- `npm.cmd run governance:check`: 67/67 tests plus governance validation pass.
- Changed JavaScript was checked with `node node_modules/eslint/bin/eslint.js` and an explicit file list including new files: zero errors.
- `git diff --check`: pass. No staged changes.
- Base regressions use Node registerHooks to load the exact HEAD production module bytes from `git show`, then run the new tests. No reset, restore, checkout or edits to base production files occurred.
- Local evidence logs under .cache/audit-evidence/: f01-base-regression.log, f01-unit-tests.log, governance-tests.log. Logs are working evidence and excluded from the intended commit scope.

## Remaining gates and limitations

The original report's exact $16,180 reproduction inputs and audited SHA were not available. This is a current-base invariant reproduction, including the production federal resolver. This does not change the shortcut-only simulation policy or the working-year consumption model. No ESLint errors or warnings in changed JavaScript.

Locked dependencies were installed using `npm.cmd ci --ignore-scripts`; the local Chrome executable and canonical port 8825 were checked. Required `npm run verify` needs an immutable artifact from a clean committed candidate (scripts/browser/artifact.mjs). It has not run on these edits. No browser/visual result is claimed from unit tests. Independent review, exact-candidate CI, bot commit/PR identity checks and owner merge approval remain pending. This is not merge-ready or live.

No commit, push, PR, merge, deployment or saved-household mutation was performed. Revert this isolated fix commit once a commit exists; no saved-data changes.
